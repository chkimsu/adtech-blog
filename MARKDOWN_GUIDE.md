# 블로그 글 작성 가이드

글 본문은 `posts/<slug>.md` **마크다운 파일**로 작성하고, 메타데이터(제목·카테고리·태그 등)는 `js/posts.js`에 둡니다. 브라우저가 marked.js로 `.md`를 받아 렌더합니다. (별도 빌드 없음)

> 예전의 `isMarkdown: true` + 인라인 `content:` 방식은 더 이상 쓰지 않습니다. 본문은 항상 외부 `.md` 파일입니다.

## 누구를 위해 쓰는가 (방향을 정하는 기준)

**글마다 두 독자를 동시에 만족시켜야 합니다.** 둘 중 하나만 보면 글이 한쪽으로 기웁니다.

1. **애드테크를 처음 보는 사람** — 개념어를 모르는 상태로 들어옵니다. 그래서 모든 절이 "구체 숫자 → 왜 그런가 → 실무에선" 순서입니다. 어려운 용어는 꼭 필요할 때만 쓰고, 쓸 때는 짧은 풀이를 붙입니다.
2. **광고 랭킹 모델을 만드는 엔지니어** — pCTR·pCVR 모델러가 읽어도 가져갈 것이 있어야 합니다. 그래서 파이썬 예제와 가데이터 계산이 필수입니다. "개념만 훑고 끝"이면 이 독자에게는 쓸 데가 없습니다.

**저자의 자리도 분명히 하세요.** 이 블로그의 저자는 **담장 안(walled garden) DSP의 pCTR/pCVR 엔지니어**입니다. 경매 안쪽에서 모델을 만드는 사람입니다.

그래서 **열린 RTB의 개념(낙찰가 미관측·bid shading·헤더비딩)은 "바깥 세계" 지식으로 다룹니다.** 직접 경험이 아니라 구조를 설명하는 것입니다. 반대로 담장 안 이야기(로그인 ID로 결정론적 매칭, 관계 데이터, 피처 스토어 운영)는 저자의 자리에서 쓸 수 있습니다.

이 구분을 흐리면 독자가 "이게 내 상황에 해당하나"를 판단할 수 없습니다. 그래서 **무대 표시**(아래)가 이 블로그의 뼈대입니다.

## 새 글 추가 — 한 흐름

```bash
# 1) 스캐폴드: posts/<slug>.md stub + js/posts.js 엔트리 생성(분류 검증)
node scripts/new-post.js
#    인자 모드: node scripts/new-post.js <slug> "<제목>" "<카테고리|번호>" "<태그,콤마>"

# 2) posts/<slug>.md 본문 작성 + js/posts.js 의 excerpt 채우기

# 3) 검증 — 셋 다 통과해야 CI도 통과
node scripts/validate-posts.js           # 메타 정합성(분류·빈 필드·없는 .md·죽은 링크)
node scripts/check-content-standard.js   # 내용 표준 대시보드 (아래 "글 내용 표준")
node scripts/build-search-index.js       # 검색 색인 재생성

# 4) chkimsu 계정으로 커밋·푸시 → sitemap·feed 자동 갱신
git add posts/<slug>.md js/posts.js search-index.json   # 파일을 명시할 것
git commit -m "feat(posts): …"
git fetch origin && git merge origin/main               # CI가 sitemap·feed를 되커밋하므로
git push origin main
```

**세 가지를 꼭 지키세요.**

- **`git add -A`를 쓰지 마세요.** 작업 중인 다른 파일이 섞여 들어갑니다(실제로 한 번 커밋을 되돌려야 했습니다). 파일을 하나씩 명시합니다.
- **push 전에 `git fetch` + `merge`** 를 합니다. CI가 `sitemap.xml`·`feed.xml`·`search-index.json`을 자동으로 되커밋하기 때문에, 바로 push하면 거부됩니다. 충돌은 나지 않습니다.
- **커밋은 `chkimsu` 계정으로** 합니다(회사 이메일 금지).

## 무대 전환 배지 (본문 안)

글 전체 무대는 `posts.js`의 `world`/`worldNote`가 담당하지만, **본문 중간에 무대가 바뀌는 섹션**은
헤딩 끝에 마커를 붙이면 제목 옆 색 배지로 렌더됩니다:

```markdown
## 3. 헤더비딩의 원리 [무대: 열린 RTB]
### 담장 안에선 어떻게 다른가 [무대: 닫힌 생태계]
```

- 허용 값: `열린 RTB` / `닫힌 생태계` / `공통` (그 외는 마커가 일반 텍스트로 노출됨)
- 목차(TOC)·검색 색인에는 마커가 나타나지 않습니다.
- 헤딩당 마커는 1개만, 제목 없이 마커만 쓰지 마세요 (목차가 배지 텍스트로 대체됩니다).

## 접이식 심화 블록 (:::deep)

수식 유도·구현 디테일·논문 같은 심화 내용은 접이 블록으로 감싸면
초심자는 건너뛰고 실무자만 펼쳐 봅니다:

```markdown
:::deep 더 깊이 — Shapley value 기반 기여 배분
여기에 마크다운 본문 (코드·수식·표 모두 사용 가능)
:::
```

제목을 생략하면 '더 깊이'가 기본값. 블록은 중첩할 수 없습니다.

## 글 내용 표준 (2026-08 확정)

시범 3편이 이 표준의 본보기입니다 — `posts/attribution-basics.md`(기준자), `posts/pctr-prediction.md`, `posts/header-bidding.md`. **새 글을 쓰기 전에 기준자 한 편을 읽고 시작하세요.**

⚠ **다만 본보기가 규칙보다 앞섭니다.** 시범 3편은 전부 2026-06-21 이고 비유 금지는 2026-08-07 확정입니다. 실제로 `posts/header-bidding.md` 첫 줄("경매장에 사람들이 모였다. 손님은 여섯 명인데…")은 지금은 금지된 방식입니다. **분량·리듬·돈 환산은 시범 3편에서 배우고, 비유 없이 쓰는 법은 `posts/gateway-ingress-router.md`·`posts/kafka-log-pipeline.md` 에서 배우세요.**

1. **구조**: 도입 장면(일상 예시, 개념어 없이) → `> 한 줄 요약:` → 본문 섹션 → 한눈 정리 표 → 헷갈리기 쉬운 점 → 더 깊이 보기
2. **섹션마다 굵은 요약 한 줄 + 상세 2~4문단.** 한두 줄로 끝나는 섹션은 두지 않습니다. 전개 순서는 구체 숫자 → 왜 그런가 → 실무에선.
3. **가데이터 최소 1개** (5~10행 표). 그 숫자로 개념을 직접 계산해 보입니다. 규칙·설정을 바꿨을 때 **같은 데이터에서 결과가 어떻게 달라지는지**까지 보이면 가장 좋습니다.
4. **파이썬 예제**: 표준 라이브러리만, 복붙해 바로 돌아가게. 작성 후 **반드시 실제로 실행**해 `# 출력:` 주석에 결과를 붙이고 본문 숫자와 맞춥니다. 본체는 20~60줄이지만 머리 주석과 `# 출력:` 이 절반을 차지해 실제로는 60~80줄이 됩니다(최신 글 13블록 중 8블록이 60줄 초과). 그게 정상입니다.
5. **주석이 곧 설명 본문입니다** — 무엇을+왜, 마지막에 출력 예시. (주석은 세이지 그린 이탤릭으로 렌더됩니다)
6. **무대가 갈리는 섹션엔 `[무대: …]` 배지**, 심화는 `:::deep`, 관련 데모는 `?embed=1` iframe으로 본문 안에.
7. **문장은 짧게 — 다만 너무 짧으면 더 어려워집니다.** 80자는 **상한**이고 **목표 평균은 44~56자**입니다(저장소 68편의 중앙값 46.7자, 사분위 43.9~55.8). 한 문장 = 한 생각이되, 설명 문장은 이어져야 이해됩니다.
8. **분량**: 얇은 글은 15KB 이상으로. 분량 자체가 목표가 아니라 1~7을 채우면 자연히 도달하는 수준입니다(시범 3편은 22~26KB, 저장소 평균 37.7KB).
9. **숫자·용어는 사실로 맞아야 합니다.** 예: COPC = 실제÷예측(1보다 크면 과소예측), 다운샘플링 보정을 빼먹으면 확률이 **위로** 부풉니다. 연도·제품명은 확인하고 씁니다.
10. **가데이터임을 밝힙니다.** 표나 코드에 쓴 숫자는 "가상 데이터"·"설명을 위해 …라고 가정하자"처럼 한 번 말해 둡니다. 실측치로 오해하면 독자가 그 숫자를 인용해 버립니다.

### 쉽게 쓰기 — 실측으로 배운 것 (2026-08-15 추가)

신규 7편을 두 번 "쉽게" 고치고서야 원인을 찾았습니다. **처음 두 번은 엉뚱한 곳을 고쳤고, 그중 둘은 역효과였습니다.** 기준자와 수치로 대조한 결과입니다.

| 축 | 어려운 글 | 기준자 3편 | 판정 |
|---|---|---|---|
| 분량 | 59.2 KB | 27.0 KB | 2.2배 |
| 본문 SVG | 2~3개 | **0개** | 표와 겹치는 그림이 절반 |
| 표 | 9~21개 | 3~5개 | 3배 |
| 산문 1,000자당 숫자 | 19.7개 | 11.6개 | 1.7배 |
| 돈·결정어 10KB당 | 0.8~10.3회 | 24~28회 | **3~30배 부족** |
| 평균 문장 길이 | 33자 | 48자 | 어려운 쪽이 **더 짧다** |
| 영어 약어 종류 | 11.4 | 13.8 | **차이 없음** |

**약어를 줄이는 건 답이 아니었습니다.** 이미 기준자와 같은 수준이었습니다.

🔴 **역효과였던 두 가지 — 되풀이하지 마세요.**

1. **"문장을 60자로 끊어라"** → 33자까지 내려가 산문이 **사실의 나열**이 됐습니다. 짧은 문장 + 높은 숫자 밀도 = 숫자 목록입니다. 목표는 **44~56자**입니다(표준 7).
2. **"수식·유도는 전부 `:::deep` 으로"** → 식만이 아니라 **논리의 마지막 단계가 같이 묻혔습니다.** 접기 34개에 글마다 "가장 어려운 절"의 심장이 들어 있었습니다. **접기에는 식·구현 세부·민감도·두 번째 시뮬레이션만 넣고 "왜"는 본문에 남깁니다.** 글당 3개 이하.
   - 검증법: **접기를 하나도 펴지 않고 1절부터 끝까지 읽어** 논리가 끊기면 실패입니다.
3. **앞머리에 「이 글에 나오는 말」 용어표를 만들지 마세요.** 1절 전에 낯선 말 6~7줄을 던져 도입이 1,363자로 불었습니다. 용어는 처음 쓰는 절에서 풉니다.

**대신 이것들을 지키세요.**

- **도입은 400~500자 · 4문단 · 표 0개.** 순서는 **사람이 한 행동 → 이야기 → 굵은 질문 한 줄 → 그 질문에 이름 붙이기(=용어 도입) → 왜 중요한가(돈·결정)**. 첫 문단의 숫자는 하나면 충분합니다. (`posts/attribution-basics.md` 1~7행이 이 리듬입니다. 사람이 하는 일이 **실제 업무**면 비유가 아닙니다.)
- **인프라·API 용어도 개념어입니다.** 애드테크 용어(pCTR·예산·폴백)만 푸는 게 아니라 API·요청/응답·QPS·지연·처리량·타임아웃도 풀어야 합니다. **도입 첫 문단부터 그 말이 필요한 글이면 1절을 「요청 한 건이 지나가는 자리」로 세우고, 그 경로 위에서 말에 이름을 붙입니다.** 용어표가 아니라 절이라는 점이 위 3번과 같이 갑니다 — 경로에는 순서와 숫자가 있어서 읽히지만 용어표에는 없습니다. 도입에서는 이름을 빼고 하는 일로 씁니다("pCTR 서버 지연" → "광고 점수를 매기는 서버가 얼마나 빨랐는지"). 표를 하나 더 늘리지 말고 실물(요청·응답 원문에 시각 주석)로 보이세요.
  - 2026-08-24 사용자 지적: *"api의 기본 개념들을 먼저 설명하고 그 다음 내용들이 설명되어야 하는데 기본 개념에 대한 설명이 아예 없어. 이 블로그는 무조건 초심자를 위한거야."* 맨 위 「누구를 위해 쓰는가」의 첫 독자가 빠진 것이었습니다. 본보기는 `posts/serving-latency-throughput.md` 1~2절입니다.
  - 🔴 **경로 절과 「재는 말」 절은 따로 세웁니다.** 같은 날 두 번째 지적이 여기서 나왔습니다 — 1절에 경로를 세우고 정작 물었던 latency·throughput·QPS 를 그 절 끝 문단에 한 문장씩 스쳐 보냈습니다. 제목에 그 이름이 없으면 "QPS 가 뭐야"로 찾아 읽을 수가 없습니다. 재는 말은 **제목에 이름을 박은 자기 절**을 갖고, 세 가지를 그 절에서 끝냅니다. ① 원어를 한 번 같이 적습니다(latency·throughput) — 실무에서 듣는 말과 연결이 안 되면 푼 게 아닙니다. ② 단위를 밝히고 왜 서로를 대신 못 하는지 씁니다("건당 ms" 대 "초당 건"). ③ **뒤 절이 쓰는 파생값의 출처를 그 절에서 산문으로 만듭니다** — 처리 한계 3,439 QPS 와 부하율 0.77 이 코드 주석 안에만 있고 뒤 다섯 곳이 정의 없이 쓰고 있었습니다.
- **한 편에 데이터는 1~2벌.** 첫 절에서 만든 표를 뒤 절이 다시 씁니다. 절마다 새 표를 만들면 독자가 절마다 새 데이터를 익혀야 합니다. 표 총 5~7개, 절당 1개.
- **한 절 = 굵은 요약 1줄 + 문단 4~6개 + 표 1개 *또는* 코드 1개.** 둘 다 필요하면 절을 쪼갭니다.
- **숫자가 7개 넘게 몰린 문단은 글당 5개 이하.** 저장소 68편의 중앙값이 3개, 상위 25%가 5개입니다. **0개를 목표로 잡지 마세요** — 실측 없이 0을 노리다 설명에서 근거를 빼게 됩니다. 그리고 **바로 위 표에 있는 값을 문장에서 다시 더하지 마세요.** 표를 본 사람에겐 중복이고 안 본 사람에겐 벽입니다.
  - 세는 대상은 **산문 문단만**입니다. SVG 라벨과 `figcaption` 은 도표이니 빼고 잽니다. 그림 라벨에 숫자가 몰리는 건 정상이고, 그걸 줄이려 그림에서 값을 빼면 그림이 쓸모없어집니다.
- **한 문장에 단위 하나.** `%p` 를 쓰지 말고 "0.809에서 0.407로" 원값 두 개로 적습니다. 배수는 "절반"처럼 말로. 소수는 2자리까지.
- **한 문단 = 한 개념.** 새 이름이 둘이면 문단을 자르고 둘째는 다음 절로 미룹니다.
- **표 칸·절 제목·굵은 요약에는 이미 본문에서 풀린 말만.** 표는 훑어 읽는 자리인데 거기가 첫 등장이면 걸립니다.
- **표가 있으면 같은 것을 그리는 SVG는 뺍니다.** 실측에서 본문 SVG 19개 중 9개가 바로 옆 표를 그림으로 다시 그린 것이었습니다. 기준자 3편이 SVG 0개인 건 우연이 아닙니다.
  - **실제 화면 캡쳐는 예외입니다** (2026-08-24 추가). 도구 화면(Grafana 패널 등)은 표로 대신할 수 없는 것이 있습니다 — 값이 아니라 "화면에서 이렇게 보인다"가 내용일 때입니다. 다만 다섯 가지를 지킵니다.
    - 🔴 **회사 화면은 캡쳐하지 않습니다.** 공개 저장소라 패널 제목·메트릭 이름·job 라벨·실측 트래픽이 한 장에 같이 나갑니다. 로컬에 같은 도구를 띄우고 글의 가상 값을 먹여 캡쳐합니다. `scripts/grafana-capture/` 가 그 하니스입니다(brew 로 prometheus·grafana, promtool 백필, 대시보드 JSON, 캡쳐 스크립트).
    - **라이트·다크 두 벌**을 만듭니다. 캡쳐는 인라인 SVG 처럼 테마 토큰을 못 따라갑니다. `css/style.css` 의 `.fig-light`·`.fig-dark` 규칙이 테마에 맞는 한 장만 보여 줍니다.
    - **폭은 1100px** — 본문(`.post-content`) 최대 폭과 같게. 더 넓게 잡으면 화면에서 줄어들어 축 글자가 안 읽힙니다(1400px 로 잡았다가 다시 뽑았습니다).
    - **그림 안에 값을 찍습니다.** Grafana 라면 범례에 Max·Last 를 켭니다. 캡션이 없어도 숫자를 읽을 수 있어야 합니다.
    - **캡션에 가데이터임을 밝히고**(표준 10번), 도구가 낸 값과 산문이 어긋나면 **산문을 캡쳐에 맞춥니다.** 실제로 창 길이 그림에서 1분 창이 산식 20.05% 대신 21.9% 로 떠서 본문 세 숫자를 고쳤습니다.
- **코드형 식별자를 사람 말로.** `r-8f21` → "메인 상단 요청". 네 자리 광고번호가 CTR 값과 섞이면 숫자 밀도가 두 배로 느껴집니다.
- **절 끝 두 문장 = 결정 + 다리.** 그 숫자가 뒤집는 결정 하나("₩4,000이면 더 붓고 ₩20,000이면 접는다"), 다음 절로 넘기는 하나. 돈·결정어를 10KB당 20회 이상 유지합니다.

### 표기 규칙 (사용자 확정)

- **비유를 쓰지 않습니다** (2026-08-07 확정). 성문·집·길·푸드코트 같은 일상 사물 비유 대신 **실제 설정·실제 값·표**로 씁니다. 인프라·시스템 주제는 보여 줄 실물이 있습니다 — Ingress 규칙 yaml, Kafka 로그 한 줄, 헬스체크 설정값. 비유를 지우면 글이 어려워질 것 같지만 실제로는 반대입니다. 비유는 "그래서 실제로 뭐가 어떻게 되는데"를 미뤄 두는 장치라, 빼면 그 자리를 구체적인 것이 채웁니다.
  - **2026-08-07 이전 글 39편은 비유로 쓰였고 소급해 고치지 않습니다.** 새 글에만 적용합니다. `posts/kubernetes-networking.md`(성문·집)와 `posts/software-architecture-patterns.md`(푸드코트)가 옛 방식의 대표이고, `posts/gateway-ingress-router.md`와 `posts/kafka-log-pipeline.md`가 새 방식의 기준자입니다.
- **`§` 금지.** 절을 가리킬 때는 `2절`, `1~3절`, `4·6·11절`처럼 한국어로 씁니다. `§`는 서양 논문 기호라 낯설고 `$`로 오독되면 수식·금액과 헷갈립니다. (2026-08-02 사용자 지적으로 39편 319곳을 일괄 교체)
- **읽는 시간 추정 금지.** "약 46분"·"5분 소요" 같은 표기를 쓰지 않습니다. 분량을 알려야 하면 **절 개수**로 씁니다 — "절이 11개인 긴 글입니다". 독자의 읽는 속도를 대신 정하지 않는다는 뜻입니다.
- **소개(About) 페이지는 없습니다.** 되살리지 마세요.
- 🔴 **사내 시스템·플랫폼 이름은 절대 쓰지 않습니다** (2026-08-14 사용자 확정). 이 저장소는 공개된 GitHub Pages라, 사내 도구 이름이 한 번 나가면 검색엔진 색인에서 되돌리기 어렵습니다. 개념은 그대로 쓰되 **공개된 제품 이름으로 바꿔 씁니다**.
  - 예: 사내 유통 계층 이름 대신 **Logstash · Kafka Connect · Debezium · Airbyte · Flink sink**.
  - 코드 주석·CSS 주석·커밋 메시지도 본문과 같은 규칙입니다. 팔레트 출처를 적을 때 사내 프로젝트명을 쓰지 마세요(실제로 한 번 들어갔다가 걷어냈습니다).
  - 나가기 전에 확인: `grep -rinE "사내이름1|사내이름2" --include="*.md" --include="*.html" --include="*.js" --include="*.css" .`
- **디자인 톤 (2026-08-14 개편)**: **각진 판 · 그림자 없음 · 1px 실선 · 넓은 폭 · 큰 글자.** 색은 정본 17색 한 벌(라이트/다크)이고 강조는 3색조입니다 — 네이비(`--navy`, 통과·링크) · 산화철(`--oxide`, 문제·강조 · 옛 벽돌색을 그대로 물려받았습니다) · 회색(`--grey`, 조건부·참고). **serif 는 쓰지 않습니다** — 제목도 Pretendard. 네온·글래스·그라데이션·글로우·이모지·장식 글자(`▶★■●`)도 쓰지 않습니다. 코드 주석은 세이지 그린 이탤릭(구문 강조는 예외로 남겨 둔 색입니다).
  - 원칙 8가지의 정본은 스킬 `html-wiki-style` 2번이고, 이 저장소용 게이트는 `node scripts/check-design.js` 입니다. **hex 를 새로 박지 말고 토큰만 쓰세요.**
  - 색이 넷 이상 필요한 차트만 `--series-1..6` 을 씁니다. 그 전에 **채움/테두리로 가를 수 있는지** 먼저 보세요.
- **상태 색은 시맨틱 변수로**: `--state-bad`(=oxide) / `--state-good`(=navy) / `--state-warn`(=grey). 라이트·다크 두 테마에서 동시에 대비를 만족하는 단일 hex는 없습니다. canvas·Chart.js에서는 `var()`가 조용히 실패하므로 `cssVar()` 헬퍼 + 폴백 상수를 씁니다.
- **채움 위 글자는 `var(--paper)`** 로 씁니다. `white` 로 박으면 다크에서 채움색이 밝아져 대비가 깨집니다(라이트=흰색, 다크=먹색이라 양쪽 다 통과).

### 엔지니어링 기초 트랙 — API·Kafka 8편만의 규칙 (2026-08-16 사용자 확정)

사용자 지적: *"kafka, api 글들이 ML 글보다 어렵다. 내용이 잘 이어지지 않는다. 파이썬은 오히려 방해가 된다. 모두 존댓말로. 카드 제목이 비유라 무슨 engineering 글인지 모르겠다."* 8편을 전면 재작성하며 정한 것들이고, **이 트랙에만 적용**합니다.

**1. 파이썬을 안 씁니다.** 표를 뽑으려고 60~80줄을 싣는 대신 아래 넷으로 대신합니다. `scripts/check-content-standard.js` 의 `NO_PYTHON_BY_DESIGN` 에 8편이 등록돼 있어 `코드없음` 플래그가 안 붙습니다.

| 그 파이썬이 하던 일 | 대체 |
|---|---|
| 표 하나를 계산해 출력 | 마크다운 표 + 바로 위에 "가정 한 줄 · 계산식 한 줄" |
| 한 줄 공식을 보임 | 본문에 그대로 — "partition 최소치 = 초당 줄 ÷ 한 명 처리량" |
| 절차를 단계별로 보임 | SVG 하나 또는 before/after 두 블록 |
| 실제 산출물을 만들어 보임 | **그 산출물을 그냥 붙임** |

마지막 칸이 핵심입니다. **코드 블록 수는 줄지 않고 파이썬이 실물로 바뀝니다** — nginx 액세스 로그 한 줄, HTTP 요청·응답 원문, 변환 전후 JSON, `logstash.conf`, Ingress yaml.

**2. 존댓말입니다.** `~합니다`/`~입니다`. `~하죠`·`~네요` 는 안 씁니다. 굵은 요약·표 캡션·figcaption·SVG 라벨까지 포함하되, 표 안의 짧은 라벨은 명사구 그대로 둡니다.

**3. 지훈 씨 한 사람이 순서대로 겪습니다.** 8편이 한 이야기이고 시간이 앞으로만 갑니다.

| 순서 | 글 | 지훈 씨가 있는 자리 |
|---|---|---|
| 1 | `api-basics` | 입사 첫 주. `POST /v1/search` 에서 막힘 → 클릭 수집 API `POST /v1/events` 를 만듦 |
| 2 | `api-kinds-and-contracts` | 둘째 주. 광고주 전환 포스트백 첫 연동 |
| 3 | `gateway-ingress-router` | 셋째 주. 자기 API 주소를 매체에 알려 주려다 붙잡힘 |
| 4 | `pipeline-push-and-pull` | 넷째 주. "내 파일 한 줄이 어떻게 네 곳에 가 닿나" |
| 5 | `log-hops-to-kafka` | 같은 클릭 한 건을 자리마다 열어 봄 |
| 6 | `kafka-log-pipeline` | 그 클릭이 브로커에 놓인 뒤 |
| 7 | `data-pipeline-design` | 티켓 — "이 로그를 운영자 검색 창에도" |
| 8 | `data-distribution-layer` | 그 티켓을 받는 쪽 |

**4. 표준 데이터 한 벌.** 8편이 같은 값을 씁니다. 새 글이 이 트랙에 붙으면 여기서 가져옵니다.

| 값 | 얼마 |
|---|---|
| 그 클릭 한 건 | 2026-08-16 16:48:21.234 · A앱 메인 상단 · 광고 `9931` · 요청 `r-8f21` |
| 바이트 사슬 | 앱 객체 110 → HTTP 본문 85 → nginx 183 → 에이전트 봉투 346 → 변환기 309 B (필드 5 → 15) |
| 볼륨 | 하루 노출 2억 2,800만(초당 2,639) · 클릭 228만(초당 26.4) · 액세스 로그 5,928만 줄(초당 686) |
| 탭 → Kafka | 1,112 ms (그중 640이 로컬 파일 대기) |
| 읽는 쪽 넷 | 대시보드 2초 · 예산 소진 확인 5초 · 광고주 리포트 5분 · 모델 학습 다음 날 새벽 |
| Kafka | topic `ad.impression`·`ad.click` · partition 12 · key `req_id` · 보존 7일 |
| 12ms | 입찰 요청 예산 (`gateway-ingress-router` 가 정함) |

⚠ **바이트를 새로 적을 때는 지어내지 말고 실제로 세십시오.** 재작성 전 두 글이 같은 클릭에 79/169/308 B 와 85/183/309 B 를 각각 쓰고 있었습니다.

**5. 어려운 개념은 이름을 빼고 사실만 남깁니다.** 기준은 *"API·Kafka 를 실무에서 이야기할 수 있는 정도"* 입니다.

| 뺀 것 | 어디 | 어떻게 |
|---|---|---|
| 멱등성 | `api-kinds` | 용어를 뺌. `Idempotency-Key` 헤더 이름을 설명하는 자리에서 한 번만 부름 |
| 지수 백오프 · 지터 | `api-kinds` | "다시 보내는 시각을 흩는다"로. 180 → 472건 숫자는 유지 |
| 인증 다섯 → 셋 | `api-kinds` | API 키 · 토큰 · 서명. mTLS·OAuth 세부는 뺌 |
| 서비스 메시 · 사이드카 · mTLS | `gateway` | 절 통째로 뺌. 마지막 절에 한 문단 |
| Avro · zigzag varint · 스키마 레지스트리 | `log-hops` | 절 통째로 뺌. "배치로 묶어 압축한다" 한 문단 |
| `acks` · `min.insync.replicas` | `kafka` | 절 뺌 → "가장 엄한 읽는 쪽에 맞춘다" 한 문단 |
| at-most-once / at-least-once | `kafka` | 영어 이름을 빼고 "유실이냐 중복이냐"로 |
| 스키마 호환 모드 이름 | `design` | BACKWARD·FULL 대신 "한쪽 방향만 본다 / 양방향으로 켠다" |

**남긴 것** — topic·partition·consumer group·offset·보존, 상태코드·목록 나눠 받기·필드 호환·인증, 배치와 스트림, 원본/정제/마트, 로드밸런서/Ingress/Gateway.

**6. 제목 첫 토막이 기술 이름입니다.** 카드에는 제목만 뜹니다(`js/main.js` 의 `renderPostCard` — excerpt 는 카드에 안 나옵니다). "목적지가 여럿일 때"가 아니라 "데이터 유통 층 — Logstash · Kafka Connect 로…" 입니다.

**7. 분량은 KB 가 아니라 산문으로 잽니다.** `check-content-standard.js` 의 KB 는 SVG 마크업을 같이 세서, 그림이 있는 글은 부풀어 보입니다. 8편의 산문은 21.0~25.9KB 로 기준자 3편(21.8~25.7KB)과 같은 폭입니다.

### 긴 글에는 "골라 읽는 법" 안내

30분 이상 걸릴 분량(대략 `##` 절 8개 이상)이면 `> 한 줄 요약:` 바로 뒤에 붙입니다. **분량을 줄이는 대신 진입 부담만 낮추는 장치입니다.**

```markdown
> **골라 읽는 법** — 절이 11개인 긴 글입니다. 처음부터 다 읽지 않아도 됩니다.
>
> - 모델 계보만 훑으려면 → 1~3절
> - 유저 행동 시퀀스(DIN·DIEN)만 → 4절
> - 고르는 기준과 서빙 제약만 → 5~6절
> - 두 무대 비교만 → 7~8절
```

두 가지를 주의하세요.

- **`>` 다음에 빈 `>` 줄을 넣고 항목은 `> - `로 씁니다.** 연속된 `>` 줄은 마크다운이 한 문단으로 합쳐 버려서, 그냥 줄바꿈만 하면 안내가 한 덩어리로 뭉칩니다.
- **절 번호를 본문의 실제 번호와 맞춥니다.** 번호가 본문에 박혀 있지 않은 글은 절 제목을 짧게 인용하세요.

### 사실 관계에서 걸린 것들 (2026-08-07 추가 — 전부 실제로 글에 실렸다가 잡힌 것)

리뷰가 잡아낸 것 중 **형식이 아니라 내용이 틀렸던** 사례들입니다. 검사기는 이런 걸 못 봅니다.

- **한 제품의 동작을 그 부류 전체의 성질로 쓰지 마세요.** 이 함정에 두 번 빠졌습니다.
  - "Ingress는 규칙을 위에서부터 훑다가 처음 맞는 줄에서 멈춘다" → **틀렸습니다.** 쿠버네티스 명세는 *가장 길게 맞는 경로*가 이기고, ingress-nginx는 경로를 길이 내림차순으로 정렬합니다. 글이 그 위에 사고 장면과 흐름도 11개 노드를 쌓아 놨었습니다.
  - "API Gateway는 위에서부터 처음 맞는 라우트가 이긴다" → **제품마다 다릅니다.** Envoy·Spring Cloud Gateway는 순서, Kong·AWS·Traefik은 구체성이나 길이입니다. 하필 쿠버네티스 Gateway API가 후자라 대비가 뒤집혀 있었습니다.
  - 쓰기 전에 **"이걸 아는 사람이 뭐라고 받아칠까"**를 한 번 통과시키세요.
- **본문에 쓴 합계는 보여 준 데이터로 검산이 돼야 합니다.** "열 곳을 다 더하면 초당 3만 건"이라 써 놓고 표에는 다섯 줄(합계 17,200)만 보인 적이 있습니다. 독자가 더해 보면 안 맞습니다. 나머지를 한 줄로 넣거나 총계를 빼세요.
- **다른 글과 숫자가 어긋나면 실무자가 바로 잡아냅니다.** Kafka 글 초고가 "초당 5만 건"을 썼는데 `ad-log-pipeline.md`는 하루 2.28억 줄(= 초당 2,639건)입니다. 새 글의 숫자는 기존 글에서 확인하고 맞추세요.
- **`yaml` 블록은 실제로 파싱돼야 합니다.** 평범한 스칼라 안에 `: `(콜론+공백)를 넣으면 `ScannerError`가 납니다. 예시가 도식이더라도 독자가 복사해 갈 줄은 문법이 맞아야 합니다. `pathType: Prefix` 같은 **진짜 필드를 잘못된 자리**에 두면 도식보다 나쁩니다.
- **결론은 단정이 아니라 계산에서 나와야 하고, 독자가 빠져나갈 길까지 막아야 합니다.** "두 요청을 같은 서버에 둘 이유가 없다"는 단정은 두 갈래로 반박됩니다(같이 둘 이유도 있고, 포트로 나눠도 됩니다). 구체적 사고를 보이고 그 우회로까지 닫으세요.
- **시뮬레이션이 서술을 반박하면 서술을 고칩니다.** 코드를 결론에 맞추지 마세요.

### 그림·색에서 걸린 것들 (2026-08-07 추가)

- **`marker` id는 글 안에서 유일해야 합니다.** 겹치면 나중 정의가 먼저 것을 덮어 화살표 색이 엉킵니다. 접두사 + 절 번호로 짓습니다(`gw3-arr`, `kf1-arr`).
- **SVG 색을 hex로 박지 마세요.** 전부 `var(--...)`. (2026-08-14 개편 전에는 팔레트가 둘·테마가 둘이라 네 조합을 다 봐야 했습니다. 지금은 팔레트가 하나로 줄어 **라이트·다크 두 조합**입니다. 규칙은 그대로입니다 — hex를 박으면 한쪽에서 안 따라옵니다.)
- **색만으로 구분하지 마세요.** `--state-bad`는 `--accent-primary`와 **같은 산화철 값**입니다(둘 다 `--oxide`). 한 그림 안에 둘이 같이 나오면 렌더에서 구분이 사라집니다. 점선·굵기·글리프를 같이 주거나, 한쪽을 `--series-*`로 옮기세요.
- **`css/style.css`의 색을 다른 역할로 재사용할 때 알파를 확인하세요.** 옅은 배경은 `color-mix(in srgb, var(--series-N) 20%, transparent)` 꼴로 씁니다. 이걸 `alpha 1` 표시색으로 가져다 쓰면 한쪽 테마에서 대비 3:1 아래로 떨어집니다. 테마별 변형을 따로 정의하세요(`--state-*`가 그렇게 돼 있는 이유입니다).
- **mermaid `classDef`에 `fill`과 `color`를 주지 마세요.** 사이트 CSS가 라벨 안쪽 `<p>` 색을 덮어써서 다크에서 대비 1.7:1이 됩니다. **테두리 색과 굵기만** 줍니다 — 굵기 차이가 색맹·흑백에서도 통하는 신호를 겸합니다.
- **그림은 본문이 못 하는 것을 담고, 뒤 문단의 반전을 미리 까발리지 않게 배치합니다.** figcaption이 본문을 되풀이하면 둘 다 값을 잃습니다.

### 자주 밟는 지뢰

- **범위 물결표(`1~3개`, `수백~수천`)는 그냥 써도 됩니다.** GFM은 물결표 한 개도 취소선으로 인정해서 한 문단에 둘 이상 나오면 그 사이가 그어졌는데, `preprocessMarkdown`이 범위로 쓰인 물결표를 엔티티로 바꿔 막습니다. 진짜 취소선이 필요하면 `~~두 개~~`를 쓰세요.
- 수식 안에 한글을 직접 넣지 마세요 (아래 KaTeX 절 참고). 간단한 관계식은 아예 굵은 텍스트로 쓰는 편이 낫습니다.
- 데모 임베드 블록은 **앞뒤 빈 줄 필수, 내부 빈 줄 금지, 안에 `$`·`**` 금지**입니다.
- **넓은 것은 스크롤 상자에 담깁니다 — 새로 만들지 마세요.** 표는 `.table-wrapper`가, 디스플레이 수식(`$$…$$`)은 `.katex-display { overflow-x: auto }`가 감쌉니다. 이 규칙이 없으면 좁은 화면에서 **페이지 전체가 가로로 스크롤**됩니다(모바일 375px에서 문서 폭이 569px까지 벌어진 사례). 새로 넓은 요소(SVG·차트)를 넣을 때는 같은 방식으로 감싸고, 모바일 폭에서 `document.scrollWidth == clientWidth` 인지 확인하세요.
- **파이썬에서 f-string과 LaTeX를 섞지 마세요.** f-string이 `\frac{}`의 중괄호를 변수로 읽어 `NameError`가 납니다. 코드로 만드는 부분과 LaTeX 문자열을 따로 두세요.
- **시뮬레이션이 내 서술을 반박하면 서술을 고칩니다.** 코드를 결론에 맞추지 마세요. 이번 개편에서 세 번 발생했고, 세 번 다 데이터 쪽이 맞았습니다(그중 하나는 글의 주제 자체가 바뀌었습니다).

### 검사기 경고를 대하는 법 (2026-08-02 추가)

`scripts/check-content-standard.js`의 경고는 **글을 고치라는 신호이지, 경고를 없애라는 신호가 아닙니다.** 이걸 헷갈려서 실제로 사고가 두 번 났습니다.

- **"80자 초과 문장"이 뜨면 그 문장을 진짜로 두 문장으로 나누세요.** 마침표를 아무 데나 끼워 넣거나, 산문을 불릿 목록으로 바꿔 회피하거나, 따옴표를 지우지 마세요.
- **차트 위젯(`class="chart-..."`) 안의 라벨에는 마침표를 붙이지 마세요.** 도표 제목·칩 라벨에 마침표가 붙으면 오타처럼 보입니다. 검사기는 이제 chart-* 위젯 텍스트를 산문에서 제외하므로 붙일 이유도 없습니다.
- **장문 판정 기준**: 문단(빈 줄) 안에서만 세고, 콜론(`:`)도 문장 경계로 봅니다. 마크다운 링크의 URL은 길이에서 뺍니다(독자가 보는 글자만 셉니다). 그래서 링크가 많은 문장이 억울하게 잡히는 일은 없습니다.
- **"짧은 섹션"이 뜨면 그림 옆에 설명 문단을 채우세요.** 위젯 텍스트는 산문으로 세지 않습니다. 그림만 있고 설명이 없는 섹션이 바로 이 경고에 걸립니다 — 초심자에게 가장 불친절한 형태이므로 반드시 채워야 합니다.
- **파이썬 블록에 `# 출력:` 주석을 붙일 때는 직접 실행한 결과만 붙이세요.** 실행이 안 되는 구조 스케치라면 첫 줄에 `# (의사코드 — 그대로 실행되지 않습니다.)` 를 달아 독자가 헤매지 않게 합니다.
- 내부 링크의 글 id를 **파일명에서 추측하지 마세요.** `posts/pCVR-modeling.md`의 id는 `pcvr-modeling`입니다. `node -e "require('./js/posts.js').posts.forEach(p=>console.log(p.id))"` 로 확인하세요. 검사기가 죽은 링크를 잡습니다.

### 장문 검사가 반대로 샌 것 둘 (2026-08-11 수정)

검사기가 **경고를 못 내던** 자리도 둘 있었습니다. 둘 다 실제로 글에 실렸다가 사람이 손으로 세어 잡았습니다.

| 샌 자리 | 무슨 일이 | 결과 |
|---|---|---|
| 인라인 코드 스팬을 `…` 한 글자로 접었다 | `` `access_log … buffer=32k;` `` 46자가 1자로 세어졌다 | 91자 문장이 통과 |
| 줄 필터의 `\*` 가 `**굵게**` 로 시작하는 산문 줄을 불릿으로 오인했다 | 강조 걷어내기보다 필터가 먼저 돌아 그 줄이 아예 안 세어졌다 | 굵은 요약 한 줄은 길이 검사를 통째로 안 받았다 |

**고친 뒤 56편에서 80자 초과 문장이 73건 나옵니다(21편에 분포).** 장문 플래그(7건 이상)가 새로 붙은 것은 `audience-segmentation`(18) · `lookalike-modeling`(8) · `git-practical-guide`(7) 셋입니다. 표본을 확인해 보니 오탐이 아니라 진짜 두 생각이 붙은 문장들입니다.

두 가지를 알아 두세요.

- **회귀 시험이 있습니다.** `node scripts/test-long-sentences.js` — 필터·치환 순서를 건드리면 여기서 걸립니다. 코드 안의 `.`·`:`(`log.retention.ms`·`acks: all`)에서 문장이 갈리지 않는지도 같이 봅니다. 갈리면 긴 문장이 조각나 오히려 숨습니다
- **긴 인용은 코드 블록으로 옮기세요.** 이제 인라인 코드도 글자를 셉니다. git 에러 메시지 같은 84자 인용을 문장 안에 인라인으로 넣으면 걸립니다. 줄일 수 없는 문장이라 회피할 것이 아니라, 세 겹 백틱 블록으로 옮기는 것이 맞는 마크다운입니다(블록 안은 산문이 아니라 길이 계산에서 빠집니다)

## 메타데이터 & 분류 (중요)

- 한 글의 메타데이터는 `js/posts.js`의 객체 하나입니다:
  `id`(=slug), `title`, `excerpt`, `date`, `categories`, `tags`, `contentUrl`.
  (`featured`, `series`는 선택)
- **`categories`·`tags`는 자유 입력이 아닙니다.** `data/taxonomy.json`의 표준 목록에 있는 값만 씁니다.
  새 분류가 필요하면 **먼저 `data/taxonomy.json`에 추가**하세요. 검증기(`scripts/validate-posts.js`)가
  표준에 없는 값(`ML Infra` ↔ `ML Infrastructure` 같은 드리프트)·빈 필드·없는 `.md`를 막습니다.
- 주제별 읽는 순서(시리즈)·"시작하기" 글은 `js/posts.js` 상단의 `series` / `startHere`에서 관리합니다.

## 마크다운 문법 치트시트

| 기능 | Markdown | 결과 |
|------|----------|------|
| 제목 | `## 소제목` | `<h2>` (TOC에 자동 수집) |
| 볼드 | `**텍스트**` | `<strong>` |
| 이탤릭 | `*텍스트*` | `<em>` |
| 링크 | `[텍스트](URL)` | `<a>` |
| 이미지 | `![alt](URL)` | `<img>` |
| 인라인 코드 | `` `code` `` | `<code>` |
| 리스트 | `- 항목` | `<ul>` |
| 인용 | `> 인용구` | `<blockquote>` |

- 같은 블로그의 다른 글로 링크: `[제목](post.html?id=<slug>)`
- 데모로 링크: `[제목](demo-xxx.html)`

## 코드 블록 · 다이어그램

- 코드: 백틱 3개 + 언어 지정 → highlight.js 하이라이팅
  ````markdown
  ```javascript
  const code = 'here';
  ```
  ````
- 다이어그램: ```` ```mermaid ```` 블록 (테마 `neutral`)

---

## 수식 작성 (KaTeX) — 한글 주의사항

수식은 `$...$` (인라인) / `$$...$$` (블록)로 작성합니다.

### ⚠ math mode 안에 한글 직접 작성 금지

KaTeX는 `$...$` 안을 LaTeX math로 파싱합니다. 한글을 그대로 넣으면 console warning이 나고
글자 간격이 깨질 수 있습니다.

**나쁜 예 (warning 발생):**
```
$E[수익] = \int 수익 \cdot p(x) dx$
```

**좋은 예 1 — 수식 밖으로 한글 빼기:**
```
$\bar{x}_a$ : arm $a$의 평균 보상 (관측된 평균 CTR)
```
(수식 안에 한글이 들어가 있지 않으므로 OK)

**좋은 예 2 — `\text{}` 사용:**
```
$E[\text{수익}] = \int \text{수익} \cdot p(x)\,dx$
```

**좋은 예 3 — `\mathrm{}` (정자체):**
```
$\mathrm{Score}_a = \bar{x}_a + \sqrt{\frac{2 \ln t}{n_a}}$
```

### 자주 쓰는 한글 처리 패턴

| 의도 | 잘못된 표기 | 권장 표기 |
|------|-------------|-----------|
| 수식 라벨 | `$수익 = ...$` | `$\text{수익} = ...$` |
| 변수 설명 | `$x$는 입력` | `$x$는 입력` (수식 밖) |
| 함수명 | `$수익(x)$` | `$\mathrm{수익}(x)$` |

> 콘솔에 `Unicode text character "X" used in math mode` 경고가 보이면, 그 수식 안에 한글이 섞여 있다는 신호입니다.

## 데모 임베드 (글 안 미니 데모)

포스트 본문 중간에 인터랙티브 데모를 iframe으로 인라인 삽입할 수 있다.
데모 페이지에 `?embed=1`을 붙이면 차트·컨트롤·해설 패널만 남는 컴팩트 모드가 된다.

```html
<div class="demo-embed-wrap">
<iframe class="demo-embed" src="demo-beta-sampling.html?embed=1" height="560" loading="lazy" title="베타 분포 샘플링 미니 데모"></iframe>
<a class="demo-embed-open" href="demo-beta-sampling.html" target="_blank" rel="noopener">↗ 전체 데모로 열기 (가이드 투어 포함)</a>
</div>
```

**규칙 (어기면 렌더가 깨진다):**

1. **블록 앞뒤로 빈 줄** 필수 — 마크다운 HTML 블록으로 인식되기 위함.
2. **블록 내부에는 빈 줄 금지** — 빈 줄이 있으면 HTML 블록이 중간에 끊긴다.
3. **블록 안에 `$`와 `**` 금지** — 수식 보호(protectMathBlocks)와 볼드 전처리(preprocessMarkdown)가
   HTML 속성/텍스트를 치환해 버린다. title 등에 필요하면 다른 표현으로.
4. `height="560"`은 JS 로드 전 폴백 — 로드 후에는 postMessage로 실제 높이에 맞게 자동 조정된다.
5. `src`는 루트 상대 경로 (`demo-*.html?embed=1`) — post.html이 루트에 있으므로 그대로 동작.

임베드 모드를 지원하려면 해당 데모가 `js/demo-edu-content.js`에 엔트리(embedKeep)를 갖고 있어야 한다.
